const rootPath: string = 'http://localhost:8080/'

export const paths = {
    products: rootPath + '',
    productImages: rootPath + 'product-image',
    backgroundImage: rootPath + 'background',
    config: rootPath + 'config',
    mainConfig: rootPath + 'mainconfig',
    api: rootPath + 'api',
    location: rootPath + 'locations',
    position: rootPath + 'updateposition',
    timetable: rootPath + 'schedule',
    categoryImages: rootPath + 'category-image',
    banner: rootPath + 'banner',
    logo: rootPath + 'logo',
    // products: rootPath + '',
}